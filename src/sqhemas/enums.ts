export enum AuthPlasceHolders{
    "mail" = "Почта",
    "login" = "Логин",
    "password" = "Пароль"
}

export enum PathPagesAuth{
    "auth" = "/auth",
    "reg" = "/reg"
}

export enum DoAuthorization{
    "do_auth" = "auth",
    "do_reg" = "reg",
}

export enum Gender{
    male = "мужской",
    female = "женский"
}